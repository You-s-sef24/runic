export const metadata = {
    title: "About Us",
    description: "Read more about Runic",
};

export default function AboutLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}