export const metadata = {
    title: "Sign in",
    description: "Sign in to Runic",
};

export default function SigninLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}