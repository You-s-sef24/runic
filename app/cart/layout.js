export const metadata = {
    title: "My Cart",
    description: "View My Cart",
};

export default function CartLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}