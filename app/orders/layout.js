export const metadata = {
    title: "My Orders",
    description: "View My Orders",
};

export default function OrdersLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}