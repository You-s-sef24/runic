export const metadata = {
    title: "Sign up",
    description: "Sign up to Runic",
};

export default function SignupLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}