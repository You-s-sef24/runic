export const metadata = {
    title: "Settings",
    description: "Edit and adjust preferences",
};

export default function SettingsLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}