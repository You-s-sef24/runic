export const metadata = {
    title: "Checkout",
    description: "Place your order",
};

export default function CheckoutLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}