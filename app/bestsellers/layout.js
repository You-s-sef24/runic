export const metadata = {
    title: "Bestsellers",
    description: "View Our Bestsellers",
};

export default function BestSellersLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}